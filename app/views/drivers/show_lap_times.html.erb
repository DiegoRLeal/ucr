<h1>Voltas de <%= @driver.driver_first_name %> <%= @driver.driver_last_name %></h1>

<table>
  <thead>
    <tr>
      <th>Número da Volta</th>
      <th>Tempo da Volta</th>
    </tr>
  </thead>
  <tbody>
    <% @driver.laps.each_with_index do |lap, index| %>  <!-- Ajuste essa parte se as voltas estiverem em outra tabela -->
      <tr>
        <td><%= index + 1 %></td>
        <td><%= lap.time %></td>  <!-- Exiba o tempo de cada volta -->
      </tr>
    <% end %>
  </tbody>
</table>
